services:
  backend:
    build: ./backend
    ports:
      - "7500:7500"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/hike_and_strike.db
